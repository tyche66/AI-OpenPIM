# Allowlist of false-positive patterns that the V1.2 secret scan script
# recognizes. Lines starting with # are comments.
#
# Each non-comment line is matched as a substring against the scan report.
# Add ONLY patterns that are intentional test fixtures / CI placeholders —
# NEVER add anything that could mask a real leak.
#
# Examples (intent only, not auto-applied):
#   - e2e test helper tokens (helpers.ts ADMIN_TOKEN / createMockToken)
#   - CI placeholder env values ("ci-placeholder")
