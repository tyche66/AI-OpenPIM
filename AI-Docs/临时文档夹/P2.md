# P2 阶段任务提示词：知识生产化与 Portal MVP
## P2 总目标
你是 OpenPIM AI Phase 2 的项目构建者/负责人。P1 已完成现有 FastAPI 内的统一只读 `Knowledge Gateway` 最小可运行切片。现在进入 **Phase 2：知识生产化与 Portal MVP**。
P2 的目标不是新增 AI 写动作，也不是做采购/销售闭环，而是把 P1 的只读智能层从“可调试、可插拔、可审计”推进到“知识生产化、可运营、可评测、可被内部 Portal 使用”的阶段：
- 建设独立 `portal/` 内部 AI 前台 MVP
- 建设生产化知识表：`knowledge_document`、`knowledge_chunk`、`knowledge_index_job`
- 建设索引 Worker/Dispatcher 基线
- 将现有 `LegacyRagRetriever` 替换/扩展为 `HybridRetriever`
- 支持精确召回、关键词召回、pgvector 语义召回、RRF 融合
- 完成来源定位、引用校验、删除/替换一致性
- 保持 P1 的只读边界：不执行业务写入、不实现 pending_action、不创建方案/报价/分享
- 修复 P1 遗留测试/环境问题，并补齐 P2 集成、权限、安全、E2E、评测门禁
- 旧 `/api/v1/ai/*` 和 P1 `/api/v1/knowledge/query` 必须兼容
---
## P2 必须先处理的 P1 遗留
P2 开发开始前，必须把以下 P1 未完成/遗留项纳入任务和验收：
1. 全量后端测试仍有 1 个失败：
   - 命令：`PYTHONPATH=. pytest`
   - 失败项：`tests/unit/test_production_bootstrap.py::test_pgvector_uses_sqlalchemy_type_without_conflicting_asyncpg_codec`
   - 现象：`database.Vector.__module__ == "sqlalchemy.sql.sqltypes"`，测试期望 `pgvector.sqlalchemy.vector`
   - P2 必须修复 pgvector SQLAlchemy 类型加载/依赖/回退策略，使该测试通过，不能通过删除测试掩盖问题。
2. DB 集成测试大量跳过：
   - 当前环境无可达测试 PostgreSQL，导致 schema、迁移、API 集成、权限、分享、销售、媒体等测试跳过。
   - P2 必须提供可复现测试数据库启动/配置路径，并至少在本地或 CI 环境跑通关键 DB 集成测试。
3. P0/P1 eval 未执行：
   - `eval/p0/evalset.jsonl` 未完成 P1 回归报告。
   - P2 必须提供评测脚本与报告输出，覆盖精确产品识别、权限、动态字段通道、无来源拒答、安全对抗、RAG 引用。
4. P1 Gateway 仍是最小切片：
   - 当前检索为 `LegacyRagRetriever` 兼容封装，不是生产化 Hybrid Retrieval。
   - 当前 Source 缺少真实页码、章节、原文打开定位。
   - 当前内部调试页在 `frontend/`，不是正式 Portal。
   - P2 必须建设生产化知识链路与独立 `portal/` MVP。
---
# 任务 1：修复 P1 遗留测试与测试环境门禁
## 角色
你是测试与基础设施负责人，负责在 P2 开始前清理 P1 遗留，确保后续知识生产化开发有可靠测试基线。
## 目标
修复 P1 全量测试失败、让 DB 集成测试可运行、建立 P2 测试执行说明。不能通过删除或放宽测试绕过问题。
## 目标文件
可能涉及：
- `backend/app/core/database.py`
- `backend/pyproject.toml`
- `backend/requirements*.txt` 或依赖声明文件
- `backend/tests/unit/test_production_bootstrap.py`
- `backend/tests/conftest.py`
- `docker-compose*.yml`
- `.env.example`
- `README.md`
- `docs/` 中测试/部署说明
- CI 配置文件，如存在
## 需求
1. 修复 `pgvector` 类型加载问题：
   - `database.Vector` 必须来自 `pgvector.sqlalchemy.vector` 或符合既有测试预期。
   - 禁止在生产路径中静默退回普通 SQLAlchemy 类型而不报警。
   - 如果测试环境缺少 pgvector，应明确 fail-fast 或提供测试替代说明，不得伪装成功。
2. 提供可复现测试数据库：
   - PostgreSQL 16 + pgvector
   - 可通过 `TEST_DATABASE_URL` 指向安全测试库
   - 数据库名必须含 `test` 或显式 `AI_PIM_TEST_DB_APPROVED=1`
3. 跑通关键测试：
   - `PYTHONPATH=. pytest`
   - 至少不再出现 P1 的 pgvector 失败。
   - DB 不可用时允许跳过 DB 集成测试，但必须有一条明确命令能在本地/CI 拉起 DB 并跑通。
4. 补齐 P2 测试说明：
   - 单元测试
   - DB 集成测试
   - 前端构建
   - Portal E2E
   - eval 回归
## 验收
- `PYTHONPATH=. pytest` 在无 DB 环境下无失败，仅 DB 类测试按设计跳过。
- 在有测试 DB 环境下，迁移、权限、产品、知识、Gateway 关键集成测试通过。
- `test_pgvector_uses_sqlalchemy_type_without_conflicting_asyncpg_codec` 通过。
- 文档写明如何启动测试 DB、如何运行全量测试。
- 不删除 P1/P2 关键测试，不降低安全断言。
---
# 任务 2：知识生产化数据模型与迁移
## 角色
你是知识数据架构负责人，负责把 P2 的通用知识表落地。
## 目标
新增生产化知识表，但保留 P1 兼容表和旧 RAG 路由。不能破坏现有 `product_manual`、`product_manual_chunk`。
## 目标文件
建议新增：
- `backend/app/models/knowledge.py`
- `backend/app/schemas/knowledge.py`
- `backend/alembic/versions/0014_knowledge_tables.py`
可能修改：
- `backend/app/models/__init__.py`
- `backend/app/core/database.py`
- `backend/tests/test_migration_schema.py`
- `backend/tests/test_knowledge_migration.py`
## 需求
新增表：
1. `knowledge_document`
   - `id`
   - `source_type`
   - `source_id`
   - `product_id`
   - `category_id`
   - `brand_id`
   - `attachment_id`
   - `title`
   - `doc_type`
   - `version`
   - `language`
   - `content_hash`
   - `parse_status`
   - `index_status`
   - `is_active`
   - `visibility`
   - `required_permission`
   - `metadata`
   - `parser_name`
   - `parser_version`
   - `last_indexed_at`
   - `effective_at`
   - `expired_at`
   - 通用审计字段
2. `knowledge_chunk`
   - `id`
   - `document_id`
   - `product_id`
   - `category_id`
   - `brand_id`
   - `chunk_index`
   - `section_path`
   - `page_from`
   - `page_to`
   - `chunk_type`
   - `chunk_text`
   - `normalized_text`
   - `text_search`
   - `embedding`
   - `token_count`
   - `embedding_model`
   - `embedding_version`
   - `content_hash`
   - `metadata`
   - 通用审计字段
3. `knowledge_index_job`
   - `id`
   - `source_type`
   - `source_id`
   - `operation`
   - `status`
   - `content_hash`
   - `pipeline_version`
   - `priority`
   - `attempt_count`
   - `max_attempts`
   - `next_attempt_at`
   - `locked_by`
   - `locked_at`
   - `heartbeat_at`
   - `error_code`
   - `error_message`
   - `trace_id`
   - `created_by`
   - `started_at`
   - `completed_at`
   - 通用审计字段
索引建议：
- `knowledge_document(source_type, source_id)`
- `knowledge_document(product_id)`
- `knowledge_document(index_status, is_active)`
- `knowledge_chunk(document_id, chunk_index)`
- `knowledge_chunk(product_id)`
- `knowledge_chunk(category_id)`
- `knowledge_chunk(brand_id)`
- `knowledge_chunk` 的全文索引
- `knowledge_chunk.embedding` 的 pgvector 索引
- `knowledge_index_job(status, priority, next_attempt_at)`
## 约束
- 不删除旧表。
- 不修改历史迁移。
- 迁移必须支持空库升级。
- downgrade 如不可完全恢复，必须明确说明不可逆部分。
- 文档正文可以入 `knowledge_chunk.chunk_text`，但不能进入普通日志或审计摘要。
- 动态字段仍不得写入 embedding 知识卡片。
## 验收
- `alembic upgrade head` 空库通过。
- `alembic downgrade -1` 行为明确且测试覆盖。
- P1 旧 RAG 表仍存在，旧 `/api/v1/ai/rag/*` 不受影响。
- 新模型可被 SQLAlchemy 导入，无循环依赖。
- 迁移测试覆盖空库、新旧库升级、索引存在性。
- pgvector 类型测试通过。
---
# 任务 3：知识文档 Provider 与产品知识卡片
## 角色
你是知识入库负责人，负责把产品和现有说明书转换为标准知识文档。
## 目标
实现可替换的 KnowledgeDocumentProvider，P2 支持从产品结构化字段生成产品知识卡片，从现有 `ProductManual` 回填知识文档。
## 目标文件
建议新增：
- `backend/app/knowledge/documents/__init__.py`
- `backend/app/knowledge/documents/base.py`
- `backend/app/knowledge/documents/product_card.py`
- `backend/app/knowledge/documents/manual_provider.py`
- `backend/app/knowledge/documents/backfill.py`
可能修改：
- `backend/app/knowledge/sources.py`
- `backend/app/models/knowledge.py`
- `backend/tests/test_knowledge_documents.py`
## 需求
1. 定义接口：
   - `KnowledgeDocumentProvider`
   - `ProductFactProvider`
   - `KnowledgeDocumentUpserter`
2. 产品知识卡片内容只能包含：
   - 产品名称
   - 产品编号
   - 品牌
   - 类目
   - 材质
   - 规格
   - 颜色
   - 标签
   - 产品描述
   - 适用场景，仅当 PIM 有字段或文档证据
3. 产品知识卡片禁止包含：
   - 面价
   - 成本价
   - 库存
   - 供应商
   - 上下架状态
   - 报价状态
   - 方案状态
   - 客户信息
   - API Key、JWT、密钥
4. 现有 `ProductManual` 回填：
   - 保留 `ProductManual` 原表。
   - 将可索引说明书生成 `knowledge_document`。
   - 将现有 chunk 或 parsed_content 生成 `knowledge_chunk`。
   - 记录 `source_type=product_manual` 和 `source_id`。
5. 内容哈希：
   - `content_hash` 控制幂等。
   - 相同内容不重复建 chunk。
   - 产品卡片根据触发字段变化重建。
## 验收
- 产品卡片中没有价格、库存、成本、供应商字段。
- 对同一产品重复运行 upsert 幂等。
- `ProductManual` 可回填到 `knowledge_document/chunk`。
- 缺正文的说明书不伪装成功。
- 单元测试覆盖敏感字段禁止、幂等、回填路径。
- DB 集成测试覆盖插入、更新、软删除过滤。
---
# 任务 4：解析、切块与索引 Job 基线
## 角色
你是知识流水线负责人，负责 P2 的索引任务基线和 Worker。
## 目标
建设最小生产化 Worker/Dispatcher，用 PostgreSQL 任务表作为真相源，可选 Redis Streams 投递。P2 必须能异步处理知识索引任务，但不要求完整大规模调度能力。
## 目标文件
建议新增：
- `backend/app/knowledge/jobs/__init__.py`
- `backend/app/knowledge/jobs/models.py`
- `backend/app/knowledge/jobs/dispatcher.py`
- `backend/app/knowledge/jobs/worker.py`
- `backend/app/knowledge/jobs/runner.py`
- `backend/app/knowledge/indexing/__init__.py`
- `backend/app/knowledge/indexing/pipeline.py`
- `backend/app/knowledge/indexing/chunker.py`
- `backend/app/knowledge/indexing/embedder.py`
- `backend/app/knowledge/indexing/status.py`
- `backend/app/scripts/knowledge_worker.py`
- `backend/app/scripts/knowledge_dispatcher.py`
可能修改：
- `docker-compose.yml`
- `Dockerfile`
- `backend/app/core/config.py`
- `backend/.env.example`
## 需求
1. Job 状态机：
   - `pending`
   - `dispatched`
   - `processing`
   - `succeeded`
   - `failed`
   - `dead`
2. Job 操作：
   - `upsert`
   - `delete`
   - `reindex`
3. Worker 行为：
   - 获取 pending job。
   - 加锁并写 `locked_by/locked_at/heartbeat_at`。
   - 执行解析/切块/embedding/upsert。
   - 成功写 `succeeded/completed_at`。
   - 失败写 `error_code/error_message`，不保存原文。
   - 超过重试次数进入 `dead`。
   - 重复消费幂等。
4. Redis Streams：
   - P2 可先实现 PostgreSQL polling。
   - 如果实现 Redis Streams，Redis 不是真相源，PG job 仍是最终状态。
   - Redis 不可用不能破坏核心 PIM。
5. 切块：
   - 目标 300-600 tokens 起步。
   - 支持章节、页码、表格 metadata。
   - 不能固定只按 600 字符粗切作为最终 P2 实现。
   - 不可靠页码可为空，但必须标注低质量。
6. Embedding：
   - 通过既有 Adapter/Model Gateway。
   - `AI_ADAPTER=none` 时 job 失败或保持 pending/degraded，不能标成功。
   - 记录 embedding 模型、版本、维度。
## 验收
- 可通过命令启动 Worker。
- 创建 `knowledge_index_job` 后可被处理到终态。
- Worker 崩溃/重复处理不产生重复 chunk。
- `AI_ADAPTER=none` 时索引不伪装成功。
- 失败不删除旧成功索引。
- 单元测试覆盖状态机、幂等、失败重试、敏感日志。
- 集成测试覆盖一次完整 manual/product_card 索引流程。
---
# 任务 5：HybridRetriever 与 RRF 融合
## 角色
你是检索负责人，负责把 P1 Legacy Retriever 升级为 P2 Hybrid Retriever。
## 目标
实现可替换的 Hybrid Retriever，支持精确召回、关键词召回、pgvector 语义召回、产品知识卡片召回和 RRF 融合。
## 目标文件
建议新增：
- `backend/app/knowledge/retrieval/hybrid.py`
- `backend/app/knowledge/retrieval/exact.py`
- `backend/app/knowledge/retrieval/keyword.py`
- `backend/app/knowledge/retrieval/vector.py`
- `backend/app/knowledge/retrieval/rrf.py`
- `backend/app/knowledge/retrieval/filters.py`
- `backend/app/knowledge/retrieval/dedup.py`
可能修改：
- `backend/app/knowledge/retrieval/base.py`
- `backend/app/knowledge/gateway.py`
- `backend/app/knowledge/sources.py`
- `backend/app/core/config.py`
## 需求
1. 检索通道：
   - 精确产品编号/型号召回
   - 标题/文档名召回
   - PostgreSQL 关键词/全文召回
   - pgvector 语义召回
   - 产品知识卡片召回
2. RRF 融合：
   - 默认公式可配置。
   - 通道候选数可配置。
   - 精确命中必须优先。
   - 同文档最多 3 个 chunk。
   - 多产品比较时每个识别产品至少尝试返回 1 个来源。
3. ACL/状态过滤：
   - 用户权限池
   - 产品状态
   - 文档 `is_active`
   - 文档 `visibility`
   - `required_permission`
   - 软删除过滤
   - `expired_at/effective_at`
4. 动态字段隔离：
   - 价格、库存、成本、供应商、上下架、报价、方案状态不得从文档证据回答。
   - 命中动态字段问题时必须转到 Tool Registry 或拒答。
5. 降级：
   - embedding 不可用时保留精确和关键词召回。
   - pgvector 查询失败时返回 `RETRIEVAL_FAILED` 降级事件，不影响结构化工具结果。
   - 无来源时设置 `insufficient_sources=true`。
## 验收
- 精确产品编号识别率在 eval 集 ≥ 98%。
- 说明书 Recall@10 ≥ 90%。
- 引用定位准确率 ≥ 95%。
- 动态字段由文档错误回答次数为 0。
- 权限不足时 source 不返回正文。
- 单元测试覆盖 RRF、去重、ACL、动态字段拦截。
- 集成测试覆盖 exact/keyword/vector 混合召回。
---
# 任务 6：Source 标准化与引用校验
## 角色
你是引用可信度负责人，负责确保每个回答来源可追踪、可校验、可打开。
## 目标
完善 `Source` schema、引用校验和 Gateway 答案后处理。模型不得自造引用。
## 目标文件
建议新增：
- `backend/app/knowledge/citations.py`
- `backend/app/knowledge/source_resolver.py`
- `backend/app/knowledge/source_access.py`
- `backend/app/api/v1/knowledge_sources.py`
可能修改：
- `backend/app/knowledge/schemas.py`
- `backend/app/knowledge/sources.py`
- `backend/app/knowledge/gateway.py`
- `backend/app/main.py`
## 需求
1. Source 字段：
   - `source_id`
   - `source_type=document/product/database_fact`
   - `title`
   - `product_id`
   - `document_id`
   - `chunk_id`
   - `page`
   - `section`
   - `quote`
   - `observed_at`
   - `access_policy`
   - `score`
   - `channel`
   - `open_url` 或可解析定位字段
2. 新增 API：
   - `GET /api/v1/knowledge/sources/{source_id}`
   - 权限复核后返回来源定位。
   - 不因曾出现在回答中就永久可见。
3. 引用校验：
   - 模型只能引用传入的 `source_id`。
   - 未知 source_id 触发 `CITATION_INVALID`。
   - 动态字段引用必须来自 `database_fact`。
   - 文档引用必须能定位到 document/chunk。
   - 引用冲突时降低置信度或拒答。
4. 安全：
   - 不在日志记录 quote 正文。
   - 无权限时 source 不返回正文。
   - source 打开接口防水平越权。
## 验收
- 每个有证据回答都有 source。
- 模型自造 source_id 被拦截。
- 文档 source 可定位 document/chunk/page/section。
- 数据库事实 source 显示 observed_at。
- 权限不足 source 正文为空或 403。
- 测试覆盖引用校验、权限复核、无效 source、动态字段通道。
---
# 任务 7：Knowledge Gateway P2 编排升级
## 角色
你是 Gateway 负责人，负责把 P2 HybridRetriever、CitationValidator、Worker 索引状态接入 P1 Gateway。
## 目标
保持 `/api/v1/knowledge/query` 契约稳定，内部升级检索和引用校验。P1 调试页和旧用例不能破坏。
## 目标文件
可能修改：
- `backend/app/knowledge/gateway.py`
- `backend/app/knowledge/planner.py`
- `backend/app/knowledge/model_gateway.py`
- `backend/app/knowledge/retrieval/*`
- `backend/app/knowledge/citations.py`
- `backend/app/knowledge/schemas.py`
- `backend/tests/test_knowledge_gateway.py`
## 需求
1. 查询流程升级：
   - Auth/Policy
   - Quota
   - Planner
   - Tool Registry
   - HybridRetriever
   - Context Builder
   - Model Gateway
   - Citation Validator
   - Digest Store
   - Metrics/Trace
   - SSE/JSON Response
2. SSE 事件保持 P1 兼容：
   - `meta`
   - `phase`
   - `answer_delta`
   - `source`
   - `products`
   - `done`
   - `error`
3. P2 可增加事件但不能破坏 P1 客户端：
   - `retrieval`
   - `citation`
   - `debug`，仅 `knowledge:debug`
4. 模型关闭：
   - 产品搜索、产品读取、产品比较、质量查询仍可用。
   - 知识问答可返回检索来源和“模型生成不可用”降级。
   - 不伪装自然语言生成成功。
5. Context Builder：
   - 不把 L3 字段默认发外部模型。
   - 不把 Prompt、原始用户问题、完整文档正文写日志。
   - Token 预算可配置。
## 验收
- P1 非流式/SSE 契约测试继续通过。
- P2 HybridRetriever 来源进入统一响应。
- 引用校验失败返回明确错误码。
- `AI_ADAPTER=none` 时核心 PIM 和只读工具路径全绿。
- `knowledge:debug` 才能看到调试字段。
- 旧 `/api/v1/ai/*` 兼容测试通过。
---
# 任务 8：Portal MVP 独立工程
## 角色
你是 Portal 前端负责人，负责建设独立 `portal/` MVP。
## 目标
新增独立 `portal/` Vue 3 + Vite + TypeScript 工程，作为内部 AI 前台，不替代现有 `frontend/` 管理后台。P1 的 `frontend` 调试页可保留为内部调试入口。
## 目标文件
建议新增：
- `portal/package.json`
- `portal/vite.config.ts`
- `portal/tsconfig.json`
- `portal/src/main.ts`
- `portal/src/App.vue`
- `portal/src/router.ts`
- `portal/src/api/index.ts`
- `portal/src/stores/auth.ts`
- `portal/src/views/Home.vue`
- `portal/src/views/Conversation.vue`
- `portal/src/components/ChatInput.vue`
- `portal/src/components/EventStream.vue`
- `portal/src/components/ProductCard.vue`
- `portal/src/components/CompareTable.vue`
- `portal/src/components/SourceCard.vue`
- `portal/src/components/ScopePanel.vue`
- `portal/src/styles/*`
可能修改：
- Nginx 配置
- Dockerfile / compose
- README
- CI 配置
## 需求
1. Portal 路由：
   - `/`
   - `/chat`
   - 登录态复用现有 JWT 机制。
   - 不建设外部公开匿名 AI。
2. 交互：
   - 首页主输入框。
   - 场景提示词：找产品、问资料、查质量、做比较。
   - 全屏对话工作区。
   - 支持停止生成。
   - 支持重试。
   - 支持清空当前临时会话。
   - 刷新后不承诺历史恢复。
3. API：
   - 只调用 `/api/v1/knowledge/query`。
   - 不直接调用 `/api/v1/ai/chat`、`/recommend`、`/rag/search`。
   - SSE 使用 `fetch + ReadableStream + AbortController`。
   - 非流式用于调试/降级。
4. 展示：
   - answer
   - products
   - comparison
   - sources
   - trace_id
   - confidence
   - insufficient_sources
   - error
5. 安全：
   - 不保存完整对话到 localStorage。
   - 不保存 Prompt、系统消息、工具结果原文。
   - 不在前端隐藏敏感字段作为安全边界，只展示后端投影结果。
   - 无权限时显示后端返回错误，不绕过。
6. 适配：
   - Desktop 1280x800
   - Mobile 393x851
   - 键盘可发送/停止
   - 屏幕阅读器可理解流式状态
   - `prefers-reduced-motion`
## 验收
- `portal` 可独立 `npm install`、`npm run build`。
- 登录后可调用 `/api/v1/knowledge/query`。
- SSE 逐步展示事件。
- 停止生成能 abort 请求。
- 产品卡不展示 `99999`，显示 `待核价`。
- unknown 库存显示 `库存待确认`。
- sources 可打开或展示定位信息。
- 不写完整会话到 localStorage。
- Desktop/Mobile 基础 E2E 通过。
---
# 任务 9：Nginx、部署与 SSE 支持
## 角色
你是部署负责人，负责让 Portal、Admin、API 和 SSE 在同域部署下工作。
## 目标
调整部署配置，使 `/` 指向 Portal，`/admin/` 指向现有管理后台，`/api/` 指向后端，SSE 不被缓冲。
## 目标文件
可能涉及：
- `nginx.conf`
- `docker-compose.yml`
- `Dockerfile`
- `frontend/` 构建配置
- `portal/` 构建配置
- `scripts/start_demo.sh`
- `scripts/stop_demo.sh`
- README/部署文档
## 需求
1. 路由：
   - `/` → portal 静态资源
   - `/admin/` → frontend 静态资源
   - `/api/` → backend
   - `/share/{token}` 继续兼容现有分享页策略，不能破坏公开分享。
2. SSE：
   - 禁用代理缓冲。
   - 设置合理超时。
   - 支持 Authorization Header。
   - 支持 POST 流式响应。
3. 安全：
   - 生产 CORS 不使用 `*`。
   - 不在镜像或构建产物写入 AI Key。
   - `.env.example` 只保留占位。
4. 回滚：
   - 可关闭 Portal，仅保留 Admin 和 API。
   - `KNOWLEDGE_GATEWAY_ENABLED` 或等价配置关闭时返回明确错误。
## 验收
- 本地 compose 可启动 portal/admin/backend。
- SSE 在 Nginx 后可正常逐帧到达。
- `/admin/` 旧管理后台可访问。
- `/api/v1/ai/*` 旧接口可访问。
- `/api/v1/knowledge/query` 可访问。
- 生产 CORS 配置有明确 origin。
- 构建产物 Secret scan 无 Key/JWT/Prompt。
---
# 任务 10：权限、安全与字段矩阵 P2 强化
## 角色
你是安全后端负责人，负责 P2 权限、ACL、字段出域、来源访问控制。
## 目标
在 P1 权限池基础上补齐 P2 文档 ACL、source 访问、Portal 权限和检索前过滤。
## 目标文件
可能涉及：
- `backend/app/knowledge/policy.py`
- `backend/app/knowledge/permission_pool.py`
- `backend/app/knowledge/field_projection.py`
- `backend/app/knowledge/source_access.py`
- `backend/app/knowledge/retrieval/filters.py`
- `backend/app/api/v1/knowledge_sources.py`
- `frontend/src/router.ts`
- `portal/src/router.ts`
- `backend/tests/test_knowledge_policy.py`
## 需求
1. 权限池继续支持：
   - admin
   - purchaser
   - sales
   - viewer
2. 文档 ACL：
   - `visibility`
   - `required_permission`
   - product/category/brand 状态
   - 用户业务权限
3. 字段投影：
   - 工具执行前
   - SQL 查询前
   - 检索前
   - 模型上下文构建前
   - 响应返回前
4. 敏感字段：
   - sales/viewer 不得访问成本价、供应商、采购条件。
   - purchaser 不得访问客户/报价明细。
   - admin 可访问高权限受控字段。
   - L3 默认不发外部模型。
5. 安全对抗：
   - Prompt 注入
   - 任意 SQL
   - Shell/URL 工具
   - 要求输出密钥
   - 要求忽略权限
   - 文档注入
## 验收
- 角色 × 工具 × 字段矩阵测试 100% 通过。
- source 水平越权测试通过。
- 模型 Mock 断言 Prompt/上下文不含 forbidden L3/L4 字段。
- SQL/Shell/URL 请求不会产生任意工具。
- `knowledge:debug` 才能看到检索分数和工具轨迹。
- 日志不含 Prompt、回答原文、文档正文、Authorization、API Key。
---
# 任务 11：可观测性、指标与运维状态
## 角色
你是可观测性负责人，负责 P2 的 Gateway、Retriever、Worker、Portal 指标和运维状态。
## 目标
补齐 P2 生产化指标，使每次查询、检索、索引、模型、引用都可观测，但不泄露高基数和敏感内容。
## 目标文件
可能涉及：
- `backend/app/knowledge/metrics.py`
- `backend/app/knowledge/tracing.py`
- `backend/app/observability/metrics.py`
- `backend/app/api/v1/observability.py`
- `backend/app/api/v1/health.py`
- `backend/app/knowledge/jobs/worker.py`
- `backend/tests/unit/test_observability_metrics.py`
## 指标
必须支持或预留：
- `pim_ai_query_total{intent,status}`
- `pim_ai_query_duration_seconds{intent}`
- `pim_ai_time_to_first_event_seconds`
- `pim_ai_active_streams`
- `pim_ai_query_cancelled_total`
- `pim_ai_degraded_total{reason}`
- `pim_ai_tool_total{tool,status}`
- `pim_ai_tool_duration_seconds{tool}`
- `pim_ai_retrieval_duration_seconds{channel}`
- `pim_ai_retrieval_candidates{channel}`
- `pim_ai_retrieval_no_result_total`
- `pim_ai_citation_count`
- `pim_ai_insufficient_sources_total`
- `pim_ai_model_requests_total{provider,model,capability,status}`
- `pim_ai_model_duration_seconds{provider,model,capability}`
- `pim_ai_tokens_total{provider,model,direction}`
- `pim_ai_estimated_cost_total{provider,model,capability}`
- `pim_ai_rate_limit_total{layer}`
- `pim_knowledge_jobs_total{operation,status}`
- `pim_knowledge_job_duration_seconds{stage}`
- `pim_knowledge_queue_depth{priority}`
- `pim_knowledge_oldest_pending_seconds`
- `pim_knowledge_dead_jobs`
- `pim_knowledge_chunks_active`
- `pim_knowledge_index_freshness_seconds`
## 约束
- 禁止 user_id、session_id、product_id、source_id 进入指标 label。
- trace/audit 可记录受控 ID，但不进入公开 metrics label。
- 日志字段白名单化。
- 错误码必须统一。
## 验收
- `/api/v1/observability/metrics` 可看到 P2 指标。
- 每个 Gateway 请求有 trace_id。
- 每个 Worker job 有 job_id/trace_id。
- 日志无 Prompt/原文/密钥。
- 模型、检索、工具、引用失败都有明确错误码。
- 指标单元测试覆盖 label 和脱敏。
---
# 任务 12：评测集、回归脚本与报告
## 角色
你是评测负责人，负责把 P0/P1/P2 质量门禁落成自动化脚本。
## 目标
补齐并运行 `eval/p0/evalset.jsonl` 和 P2 RAG/Portal 评测，生成报告。P2 退出不能只看接口是否 200。
## 目标文件
建议新增或修改：
- `eval/p0/evalset.jsonl`
- `eval/p1/evalset.jsonl`
- `eval/p2/evalset.jsonl`
- `scripts/p0/ai_baseline.py`
- `scripts/p1/knowledge_gateway_eval.py`
- `scripts/p2/rag_eval.py`
- `scripts/p2/security_eval.py`
- `scripts/p2/portal_e2e.sh`
- `eval/reports/*`
## 需求
评测样本覆盖：
- 精确产品/型号
- 场景选型
- 多产品比较
- 说明书问答
- 数据治理
- 采购权限差异
- 安全对抗
- 无答案/歧义
- 动态字段从结构化通道回答
- source 引用有效性
报告包含：
- 产品实体识别率
- Recall@10
- 引用准确率
- 无答案识别
- 权限字段泄露次数
- 动态字段文档误答次数
- 延迟 P50/P95
- 模型关闭降级结果
- 测试环境、commit、时间
## 验收
- `eval/p0/evalset.jsonl` 可运行并生成报告。
- P1 Gateway 回归脚本可运行。
- P2 RAG 评测脚本可运行。
- 产品实体精确识别率 ≥ 98%。
- 说明书 Recall@10 ≥ 90%。
- 引用定位准确率 ≥ 95%。
- 动态字段通道合规率 100%。
- 权限与敏感字段测试 100% 通过。
- 安全对抗集阻断率 100%。
---
# 任务 13：Portal E2E 与移动端验收
## 角色
你是前端测试负责人，负责 Portal 的 E2E、可访问性和移动端门禁。
## 目标
用 Playwright 或现有 E2E 工具覆盖 Portal 关键路径，确保桌面和移动端可用。
## 目标文件
建议新增：
- `portal/tests/e2e/*.spec.ts`
- `portal/playwright.config.ts`
- `scripts/p2/portal_e2e.sh`
可能修改：
- CI 配置
- Docker compose 测试服务
## E2E 场景
1. 登录后进入 Portal 首页。
2. 输入产品搜索问题，收到 `meta/phase/answer_delta/products/done`。
3. 输入知识问题，显示 sources。
4. 输入质量治理问题，显示 issues/products。
5. 停止生成能 abort。
6. `AI_ADAPTER=none` 时显示降级，不伪装成功。
7. sales 不显示 cost/supplier。
8. admin 可显示允许字段。
9. source 打开权限复核。
10. 移动端 393x851 下输入、流式、产品卡、来源可用。
## 验收
- Desktop 1280x800 E2E 通过。
- Mobile 393x851 E2E 通过。
- 停止生成测试通过。
- 不写完整对话到 localStorage。
- axe 或基础可访问性检查无严重问题。
- Portal build 通过。
---
# P2 总体验收门槛
P2 完成必须满足：
1. P1 遗留 pgvector 全量测试失败已修复。
2. `PYTHONPATH=. pytest` 无失败。
3. 有 DB 环境下关键集成测试通过。
4. `portal/` 独立工程可构建并可登录使用。
5. `/api/v1/knowledge/query` P1 契约保持兼容。
6. `knowledge_document/chunk/index_job` 迁移完成。
7. Worker 可处理 upsert/delete/reindex job。
8. HybridRetriever 支持 exact/keyword/vector/product_card/RRF。
9. Source 可定位、可权限复核、可引用校验。
10. 动态字段不从文档回答。
11. 结构化事实准确率 100%。
12. 产品实体精确识别率 ≥ 98%。
13. 说明书 Recall@10 ≥ 90%。
14. 引用定位准确率 ≥ 95%。
15. 权限和敏感字段测试 100% 通过。
16. 安全对抗集阻断率 100%。
17. `AI_ADAPTER=none` 时核心 PIM 和只读工具路径全绿。
18. 旧 `/api/v1/ai/*` 保持兼容。
19. 不保存原始对话，不记录 Prompt/回答原文/知识块正文/密钥。
20. Portal Desktop/Mobile E2E 通过。
21. SSE 在 Nginx 后逐帧可用。
22. P0/P1/P2 eval 脚本可生成报告。
---
# P2 明确不做
- 不实现 pending_action。
- 不创建方案草稿。
- 不修改报价状态。
- 不创建分享。
- 不执行任何业务写入动作。
- 不接 ERP/CRM/WMS。
- 不引入 Elasticsearch、独立向量库、Kafka、Kubernetes。
- 不让模型生成任意 SQL/Shell/URL 工具。
- 不让前端隐藏敏感字段作为安全边界。
- 不保存完整会话历史。
- 不对外宣称 AI 已具备采购/销售闭环。